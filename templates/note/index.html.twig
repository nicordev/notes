{% extends 'base.html.twig' %}

{% block title %}Notes{% endblock %}

{% block body %}
<div class="content">
    <h1>Notes</h1>
    <p><a href="{{ path("notes_create") }}">Add a note</a></p>
    <ul>
        {% for note in notes %}
            <li><a href="{{ path("notes_show", {id: note.id}) }}">{{ note.title }}</a></li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
